javascript:(function () {const e = window.getSelection().toString().trim();const dataList = e.split('\n');let textToCopy = '';let tempText = '';const length = dataList.length;const wordLimit = 3;let wordCount = 0;for (let i = 0; i < length; i++) {const current = dataList[i];const nextIndex = i + 1;if(nextIndex >= length) {tempText += current + "\"";textToCopy += tempText;break;}const next = dataList[i + 1];if(next === ""){wordCount++;tempText += current + ' ';if (wordCount === wordLimit) {tempText += '\n';wordCount = 0;}i += 1;continue;}if(next !== ""){if(textToCopy.length > 0) textToCopy += tempText.slice(0, -1) + '\"\n';textToCopy += '\"';tempText = current +'\n';wordCount = 0;}}navigator.clipboard.writeText(textToCopy).then(function () {console.log('Text copied to clipboard: ' + textToCopy);}).catch(function (error) {alert('Failed to copy text to clipboard: ' + error);});})();